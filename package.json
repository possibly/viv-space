{
  "name": "viv-chronicle-visualizer",
  "version": "0.1.0",
  "description": "Static visualizer for Viv run chronicles",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/bundle.mjs",
    "dev": "node scripts/dev.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "esbuild": "^0.21.0"
  }
}
